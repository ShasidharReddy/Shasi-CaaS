apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: shasi-portfolio
spec:
  template:
    spec:
      containers:
        - image: gcr.io/PROJECT_ID/shasi-portfolio:latest
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: 256Mi
              cpu: '1'
